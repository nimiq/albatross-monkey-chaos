{
  "name": "monkey-chaos",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/onmax/albatross-monkey-chaos.git",
  "author": "onmax",
  "license": "MIT",
  "scripts": {
    "chaos": "tsup src/main.ts --format esm --dts --out-dir dist && node ./dist/main.mjs",
    "bls": "tsup src/bls.ts --format esm --dts --out-dir dist && node ./dist/bls.mjs"
  },
  "dependencies": {
    "nimiq-rpc-client-ts": "^0.0.22"
  },
  "devDependencies": {
    "@types/node": "^18.15.5",
    "tsup": "^6.7.0",
    "typescript": "^5.0.2"
  }
}
